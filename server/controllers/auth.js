const mainController = require('./main')
const auth0 = require('auth0-js')

module.exports = {
  handleLogin: (req, res) => {

    console.log('req', req);
    console.log('res', res);
    console.log('json', json);
  }
}
